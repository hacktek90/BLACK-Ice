{
  "name": "black-ice",
  "version": "1.0.0",
  "description": "BlackICE Portal - The Free Web OS & Cloud Workspace",
  "license": "ISC",
  "scripts": {
    "build": "echo 'No build step required for static site'",
    "dev": "echo 'Use a local server to run the site'"
  },
  "dependencies": {
    "@sentry/browser": "^10.43.0",
    "@tailwindcss/cli": "^4.2.1",
    "@vercel/analytics": "^1.6.1"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.2"
  }
}
